Pipeline engine: 13 Python modules, ~4500 LOC. Core chain: models -> loader -> validator -> state -> slot_registry -> gate_checker -> runner. Plus: nl_matcher, observer, slot_contract, enforcer, ov_context_router, __init__. OpenViking integration via ov_context_router.py (subprocess CLI calls, no Python imports).
