Pipeline engine: 15 Python modules, ~5500 LOC. Core chain: models -> loader -> validator -> state -> slot_registry -> gate_checker -> runner. Plus: nl_matcher, observer, slot_contract, enforcer, ov_context_router, project_planner, pipeline_generator, __init__. Meta-orchestration via project_planner.py (blueprint data model) + pipeline_generator.py (generates pipelines from blueprints). OpenViking integration via ov_context_router.py (subprocess CLI calls, no Python imports).
